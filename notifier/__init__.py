"""GAS → Python hybrid notification service.

Thin wrapper that forwards Google Form submissions to a Telegram
group/topic. Companion to the prakombot project; lives at
`prakombot/notifier/`.

Public surface lives in `notifier.app` and `notifier.main`.
"""
