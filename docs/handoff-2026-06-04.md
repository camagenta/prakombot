HANDOFF CONTEXT
===============

USER REQUESTS (AS-IS)
---------------------
- "What did we do so far?"
- "ok what next todo"
- "no 1 ditaruh di mana?"
- "sudo ./notifier/deploy/setup-prod.sh"
- "kalo sudah di jalankan bagaimana? .env kebentuk atau saya harus buat sendiri lokasinya dmn?"
- "pusdiklatbps4@kurniamac prakombot % curl -s .../getMe" -> token
- "pusdiklatbps4@kurniamac prakombot % curl -s .../getUpdates" -> chat_id/thread_id
- "iya ke situ" -> confirm group topic
- (various nginx + systemd debugging commands)
- "hostname kotakpasir.my.id dan *.kotakpasir.my.id apakah ok?"
- "coba cek lagi terakhir kita masih kendala dmn dan fixnya"
- "error nih masihan, coba jalanin deh dan cek ada kendala dmn ini"
- "gunakan ssh juragan untuk akses"
- "handoff dulu lanjut besok"

GOAL
----
Complete the remaining GAS deployment step (paste code + set Script Properties + run installTrigger).

WORK COMPLETED
--------------
- Built entire notifier service (30 files) inside prakombot/ subdirectory: FastAPI webhook receiver, HMAC auth, pydantic schemas, Telegram httpx client with retry, Config singleton, health endpoint
- Wrote 50 unit tests (all GREEN) — 16 existing prakombot tests also pass, total 66/66
- Fixed 3 critical deploy bugs found by background agents: form-bot.service paths, main.py exception type, conftest collision (importing from notifier conftest instead of prakombot conftest)
- Added new tests: Config.validate(), _int_env non-integer, TelegramService connection retry, handle_form_submit routing
- Added fastapi.responses mock to conftest for route handler tests
- Cloned repo to VPS (path originally ~/prakombot, moved to /opt/prakombot)
- Installed python3-venv and created .venv with pip deps
- Created Cloudflare Origin CA cert for pb.kotakpasir.my.id and installed at /etc/ssl/certs/cloudflare-origin.pem + /etc/ssl/private/cloudflare-origin.key
- Fixed nginx.conf: removed http2 directive (nginx 1.24 ubuntu doesn't have http2 module)
- Ran setup-prod.sh with user's actual values: bot_token, chat_id=-1001936999792, thread_id=25 (topic "Keanggotaan dan pengembangan sdm" in group "Pengurus PrakomID 2022")
- Fixed form-bot.service paths (WorkingDirectory /opt/prakombot, ExecStart /opt/prakombot/.venv/bin/python)
- Started service via systemd — currently active (running), health endpoint returns {"status":"ok"}

CURRENT STATE
-------------
- Notifier service is RUNNING on VPS (43.156.90.61, hostname VM-8-28-ubuntu)
- Health endpoint: https://pb.kotakpasir.my.id/healthz -> {"status":"ok"}
- Nginx reverse proxy working (HTTPS via Cloudflare Origin CA, proxy_pass to 127.0.0.1:8000)
- Env file at /etc/prakombot/notifier.env (generated by setup-prod.sh)
- WEBHOOK_SECRET = 5165cd1138e3f40e03b8071ef8bbdc903dc1f461475584bce728aedfde1478af
- SSH accessible via "juragan" host in ~/.ssh/config (key ~/.ssh/juragandata)
- All 66 tests pass in dev environment
- 6 commits pushed to origin/main (deployed files may be slightly ahead with uncommitted fixes)

PENDING TASKS
-------------
- GAS deployment (last remaining step):
  1. Open script.google.com -> project linked to the Google Form
  2. Project Settings -> Script Properties -> add:
     - WEBHOOK_URL = https://pb.kotakpasir.my.id/webhook/form
     - WEBHOOK_SECRET = 5165cd1138e3f40e03b8071ef8bbdc903dc1f461475584bce728aedfde1478af
  3. Paste notifier/gas/sendTele_v2.gs into editor (replace old sendTele.gs)
  4. Run installTrigger() function (authorize once)
  5. Submit a test form response, verify it appears in topic Keanggotaan dan pengembangan sdm
- Smoke test webhook endpoint manually:
    BODY='{"form_id":"test","submitted_at":"2026-06-04T10:00:00Z","responses":[{"index":0,"title":"Nama","answer":"Budi"}]}'
    SIG=$(echo -n "$BODY" | openssl dgst -sha256 -hmac "5165cd1138e3f40e03b8071ef8bbdc903dc1f461475584bce728aedfde1478af" | awk '{print $2}')
    curl -s -X POST https://pb.kotakpasir.my.id/webhook/form -H "Content-Type: application/json" -H "X-Signature: $SIG" --data "$BODY"
- Optional: add bot PrakomID_bot as admin to group "Pengurus PrakomID 2022" with "send messages" permission if not already done
- Uncommitted changes on dev machine: form-bot.service, nginx.conf, main.py, conftest.py, all test files (should be committed)

KEY FILES
---------
- notifier/__init__.py — package marker
- notifier/app.py — FastAPI app factory wiring POST /webhook/form + GET /healthz
- notifier/main.py — uvicorn entrypoint with Config.validate() fail-fast
- notifier/config.py — Singleton Config with _int_env safety, validate() raises RuntimeError
- notifier/auth.py — HMAC-SHA256 verify_signature, timing-safe compare
- notifier/schemas.py — pydantic FormSubmitPayload, format_message(), find()
- notifier/services/telegram.py — TelegramService (httpx async, retry 5xx/429, hard-fail 4xx)
- notifier/routes/webhook.py — process_form_submit + handle_form_submit (FastAPI route)
- notifier/tests/conftest.py — sys.modules mock injection (httpx with real HTTPError exception class, fastapi, uvicorn, pydantic)
- notifier/deploy/form-bot.service — systemd unit (WorkingDirectory=/opt/prakombot, ExecStart=/opt/prakombot/.venv/bin/python -m notifier.main)

IMPORTANT DECISIONS
-------------------
- Production path: systemd + nginx + venv (no Docker). Dockerfile kept as alt.
- Domain: pb.kotakpasir.my.id behind Cloudflare. TLS via Cloudflare Origin CA cert (15yr).
- Webhook URI: POST /webhook/form (not /webhook alone — future-proof for other webhooks).
- HMAC secret dedicated (WEBHOOK_SECRET), generated by setup-prod.sh via openssl rand -hex 32.
- Env file at /etc/prakombot/notifier.env (mode 0640, root:notifier), managed by setup-prod.sh.
- SSH to VPS via "juragan" host alias (43.156.90.61, user ubuntu, key ~/.ssh/juragandata).

EXPLICIT CONSTRAINTS
--------------------
- Bot token must be in env (VPS TELEGRAM_BOT_TOKEN), never in source.
- Token moves from GAS Script Properties to VPS env.
- No SSH access to VPS from my side — user must run commands.
- nginx 1.24.0 on VPS does NOT have http2 module (remove http2 directives).
- Repo moved to /opt/prakombot on VPS (not ~/prakombot).
- .venv created at /opt/prakombot/.venv (not /opt/prakombot/notifier/.venv).

CONTEXT FOR CONTINUATION
------------------------
- All notifier code is final and tested (66/66 GREEN). The only remaining work is GAS deployment.
- The VPS is fully set up: nginx, cert, systemd service, .venv, env file — all running.
- User can use "ssh juragan" to access the VPS from this dev machine.
- WEBHOOK_SECRET for GAS: 5165cd1138e3f40e03b8071ef8bbdc903dc1f461475584bce728aedfde1478af
- If starting new session: uncommitted changes to notifier/deploy/form-bot.service, notifier/deploy/nginx.conf, notifier/main.py, notifier/tests/* should be committed.
